[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn main.wsgi:application --bind 0.0.0.0:$PORT --workers 2"
healthcheckPath = "/api/"
healthcheckTimeout = 30

[[deploy.releaseCommand]]
command = "python manage.py migrate --noinput"